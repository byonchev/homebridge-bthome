{
  "watch": ["src"],
  "ext": "ts",
  "ignore": [],
  "exec": "tsc && homebridge -I -U ./test/hbConfig -D",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings --force-node-api-uncaught-exceptions-policy=true"
  }
}
